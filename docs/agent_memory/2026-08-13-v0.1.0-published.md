# v0.1.0 standalone Windows release

The first public standalone release is `v0.1.0`. GitHub Actions validated the
backend suite, frontend lint/component/build/E2E suites, then published the
Windows x64 ZIP, `install.ps1`, and `release.json` checksum manifest. The
repository uses the MIT license and the release workflow is the canonical way
to produce future installable artifacts.
