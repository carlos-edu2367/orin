# Release v0.1.8

- A versão de release precisa permanecer alinhada em `pyproject.toml`, `uv.lock`, `desktop/package.json` e nas duas declarações de versão do `desktop/package-lock.json`.
- O workflow de publicação valida a versão do `desktop/package.json` contra a tag e gera os artefatos Windows a partir da tag `v0.1.8`; a tag deve ser enviada depois do commit.
- O commit anterior `0105a63` estava em `main` local, mas ainda não em `origin/main`; ele será incluído no push junto com o commit desta release.
- O diretório local `data/` contém banco, uploads, workspaces e locks de execução; permanece fora do versionamento e não deve ser incluído no release commit.
