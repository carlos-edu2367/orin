# Local personal-installation profile. The launcher supplies an absolute SQLite
# URL under Orin's data directory and generates the encryption key on first run.
DATABASE_URL=
AGENTOS_ENV=local
LOCALHOST_TRUST_ENABLED=true
WEB_DIST_DIR=frontend/dist
AGENTOS_PROVIDER_ENCRYPTION_KEY=
